-- «Дата по документам» у события аренды: дата оплаты из операции, дата выдачи, дата приёма при завершении.
-- date у события — фактический момент записи; doc_date — деловая дата, которую указал сотрудник.
ALTER TABLE rental_events ADD COLUMN doc_date TIMESTAMP;
