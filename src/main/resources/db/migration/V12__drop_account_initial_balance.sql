-- Начальный остаток у счёта убираем: баланс = сумма операций, ничего не храним.
-- Стартовый остаток вводится приходной операцией (например, статья «Начальный остаток»).
ALTER TABLE finance_accounts DROP COLUMN initial_balance;
