-- Стратегия переплаты хранится на операции оплаты выкупа: график платежей пересчитывается
-- с нуля (replay) при приёме/правке/удалении оплат — удалённая ошибочная оплата выпадает
-- из истории, и график возвращается к виду «как будто её не было».
ALTER TABLE finance_transactions ADD COLUMN overpayment_strategy VARCHAR;
